# 🧠 Fynance AI - Frontend MVP

Cette interface Streamlit permet de tester la connexion avec le backend FastAPI déployé sur Render.

## 📦 Installation locale

```bash
cd frontend
pip install -r requirements.txt
